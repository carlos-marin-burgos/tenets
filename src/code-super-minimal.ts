// Absolute minimal version - no HTML strings
console.log("Starting super minimal plugin");

// Just close the plugin immediately
figma.closePlugin();

console.log("Plugin should close immediately");
